# Gate 1 browser evidence

The PNG files in this directory are generated from the local synthetic Gate 1
fixture site. They contain no production data, external-system credentials, or
live operating metrics. The final role-specific browser matrix revalidated the
same committed PWA source against the formal four-App runtime; screenshots were
not recaptured merely to change the underlying image digest.
